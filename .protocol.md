# FRC-2025 Protocol

Project-specific protocol for FRC-2025. Universal directives (team-lead, read-on-entry, single-Read, [ACC] tag, interaction modes, Constraints) are inherited from the universal `.protocol.md` — only project-specific rules live here.

Spec (authoritative for `§` refs): `C:\GitHub\.plans\agent-context-system-design.md`.

<!-- @always -->
## Critical rules (FRC-2025)

- This repo is **archived / dormant** (2025 season complete). Do not write new code or modify existing subsystems without explicit user instruction.
- To build, set `JAVA_HOME="C:/Users/Public/wpilib/2026/jdk"` (WPILib JDK 17) — system Java is Java 8 and will fail. Build from `FRC-2025/2025Robot/` sub-directory.
- Reference code from `main` branch only; `2025_Archive` and `2025_Offseason_archive` branches are deprecated (old CANSparkMax API, not valid for 2026+).
- When pulling lessons forward to FRC-2026: verify motor API currency — architecture patterns (command factories) are incomplete on main.
- Lessons note: `frc-2025-lessons-reefscape-202606120638` — read before making FRC-2026 control/swerve decisions.
<!-- /@always -->

---

This file defines the local workflow for this repository.

This repo must remain usable on its own, without requiring `C:\GitHub` to exist.
If broader coordination context is available, `C:\GitHub\.protocol.md` is the preferred top-level entry point, but it is optional.

## Read Order

1. `.changelog.md`
2. `.project-context.md`
3. `.standards.md`
4. `.protocol.md`

## During Work

- Follow `.standards.md`
- Check `.changelog.md` before overlapping work
- Keep `.project-context.md` current when state changes

## Logging

Use `.changelog.md` with this format:

```text
### [YYYY-MM-DD HH:MM] AGENT_NAME [ACTION_TYPE]
- Description of changes
- Repo: FRC-2025
- Files modified: <paths from repo root>
- Notes: Important context for other agents
- Why: Why this change was made
- PENDING: (optional) Follow-up work
```

## Working Files

- Use `.scratch/` for temporary scripts, debug artifacts, and working files
- Use `.research/` for reference materials, external tools, and research
- Both directories are gitignored — do not commit their contents

## Incomplete Work

Track incomplete work in `.project-context.md`:

- `TODO` for actionable follow-up
- `Pending Decisions` for unresolved choices
